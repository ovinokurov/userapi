type User {
    id: ID!
    firstName: String!
    lastName: String!
}

type Query {
    users: [User!]!
}

type Mutation {
    createUser(firstName: String!, lastName: String!): User!
    deleteUser(id: ID!): Boolean!
}